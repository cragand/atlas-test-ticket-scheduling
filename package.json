{
  "name": "atlas-test-ticket-scheduling",
  "version": "1.0.0",
  "description": "Bolt for JavaScript custom step for the Atlas Test Ticket Submission workflow - self-hosted (Socket Mode), since Amazon's grid blocks next-gen (Deno SDK / Run on Slack) apps",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node src/app.ts",
    "test": "jest"
  },
  "dependencies": {
    "@slack/bolt": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "brace-expansion": "^5.0.8"
  }
}
